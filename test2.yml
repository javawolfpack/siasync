---
!!map {
  ? !!str "Sync"
  : !!seq [
    !!map {
      ? !!str "archive"
      : !!bool "false",
      ? !!str "dataPieces"
      : !!int "10",
      ? !!str "excludeExtensions"
      : !!seq [
        !!str "jpg",
        !!str "ppt",
      ],
      ? !!str "includeExtensions"
      : !!seq [
        !!str "png",
        !!str "nef",
      ],
      ? !!str "name"
      : !!str "test",
      ? !!str "parityPieces"
      : !!int "30",
      ? !!str "path"
      : !!str "./test",
      ? !!str "prefix"
      : !!str "test",
    },
    !!map {
      ? !!str "archive"
      : !!bool "true",
      ? !!str "dataPieces"
      : !!int "10",
      ? !!str "excludeExtensions"
      : !!seq [
        !!str "jpg",
        !!str "ppt",
      ],
      ? !!str "includeExtensions"
      : !!seq [
        !!str "png",
        !!str "nef",
      ],
      ? !!str "name"
      : !!str "test2",
      ? !!str "parityPieces"
      : !!int "30",
      ? !!str "path"
      : !!str "./test",
      ? !!str "prefix"
      : !!str "test",
    },
  ],
  ? !!str "version"
  : !!str "1.0.0",
}
